<h1>Movie Searching API</h1>

<a href="/">Search Again</a>

<ul>
    <% data["Search"].forEach(function(movie){ %>
        <li>
            <strong><%= movie["Title"] %></strong> - <%= movie["Year"] %>
        </li>
        <img src=<%= movie["Poster"] %>>
    <% }) %> 
</ul>